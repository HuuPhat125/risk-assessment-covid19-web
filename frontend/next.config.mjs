// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // <--- dòng này là bắt buộc để next export hoạt động
};

export default nextConfig;
